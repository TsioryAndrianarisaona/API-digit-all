package com.digitall.api.repository.document;

import com.digitall.api.model.document.FolderDocument;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FolderDocumentRepository extends JpaRepository<FolderDocument,Long> {
}
